# 25 for '25 — Movie Rankings

Published frontend for a two-person movie ranking/comparison app. Talks to a
Google Apps Script Web App backend (see `config.js`).

Source and full spec live in the private `Tools_of_Rock` repo, under
`movie-rankings/`. This repo exists only so GitHub Pages has a public
place to serve the static frontend from.
